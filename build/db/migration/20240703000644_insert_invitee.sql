-- +goose Up
CREATE TABLE invitees (
   id INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
   family_kj VARCHAR(255) DEFAULT NULL,
   first_kj VARCHAR(255) DEFAULT NULL,
   family_kn VARCHAR(255) DEFAULT NULL,
   first_kn VARCHAR(255) DEFAULT NULL,
   email VARCHAR(255) DEFAULT NULL,
   zip_code VARCHAR(255) DEFAULT NULL,
   address_text VARCHAR(255) DEFAULT NULL,
   user_id INT UNSIGNED NOT NULL,
   allergy VARCHAR(255) DEFAULT NULL,
   file_url VARCHAR(255) DEFAULT NULL,
   created_at TIMESTAMP DEFAULT NULL,
   updated_at TIMESTAMP DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
CREATE INDEX invitation_id on invitees (id);

-- +goose Down
DROP INDEX invitee_id;
DROP TABLE invitees;